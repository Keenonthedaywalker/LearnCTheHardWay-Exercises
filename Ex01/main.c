#include <stdio.h>

/* This is a comment */
// This is the complete main function.
// The first parameter is an integer that takes the argument count
// The second parameter is an array of char *strings for the arguments themselves.
int main(int argc, char *argv[])
{

    int distance = 100;

    // This is also a comment
    printf("You are %d miles away.\n", distance);
    printf("Hopefully this is more complext than Hello World.\n");
    printf("Can I take this higher?\n");
    printf("Something very interesting is happening here.\n");
    printf("123123123123\n");
    printf("Test\n");

}